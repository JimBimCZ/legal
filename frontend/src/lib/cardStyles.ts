/**
 * Shared styling for the clickable card grids used by DocumentMenu (catalog
 * types) and Dashboard (saved documents).
 *
 * Cards are uniform. The mono docket code (NDA, CSA, DPA...) identifies a type
 * on its own, so nothing is gained by giving each one its own treatment.
 */
export const cardGridClassName = "grid grid-cols-1 gap-3 sm:grid-cols-2";

export const cardButtonClassName = "ui-card group";

export const cardTitleClassName = "type-display text-base text-heading";
